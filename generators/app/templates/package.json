{
  "name": "<%= name %>",
  "version": "<%= version %>",
  "description": "<%= description %>",
  "keywords": <%- JSON.stringify(keywords) %>,
  "authors": <%- JSON.stringify(authors) %>,
  "dependencies": {
    "serve-static": "^1.10.0",
    "commander": "^2.9.0",
    "finalhandler": "^0.4.1"
  }
}
